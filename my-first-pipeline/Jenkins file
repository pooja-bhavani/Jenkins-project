pipeline {
    agent {
      docker { image 'node:17-alpine' }
}
stages {
  stage('Test') {
    steps {
      sh 'node --version'
  }
}
  stage('deploy') {
    steps {
      sh 'kubectl apply -f deployment.yml'
     }
    }
  }
}
  stage('Test') {
    steps {
      sh 'node --version'
     }
   }
 } 
}
